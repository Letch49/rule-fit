[tool.poetry]
name = "rulefit"
version = "0.1.0"
description = ""
authors = ["roman"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.9"
scikit-learn = "^1.5.0"
pandas = "^2.2.2"
mlxtend = "^0.23.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
